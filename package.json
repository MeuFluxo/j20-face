{
  "name": "j20-face",
  "version": "1.0.0",
  "private": true,
  "description": "Página estática — Jejum 2.0",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check config.js && node --check script.js && node --check player.js && npm test"
  }
}
